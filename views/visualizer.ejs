<div>
  <% if(jsonArray && jsonArray.length > 0) { 
    let keys = Object.keys(jsonArray[0]); %>
    <table id="data-table">
      <caption><%= name %></caption>
      <thead>
        <tr>
          <% for(let i = 0; i < keys.length; i++){ %>
            <th>
              <%= keys[i] %> <p onclick="sort('<%= i %>')">sort</p>
            </th>
          <% } %>
        </tr>
      </thead>
      <tbody>
        <% for(jsonObj of jsonArray){ %>
          <tr>
            <% for (key in jsonObj) { %>
              <td>
                <%= jsonObj[key] %>
              </td>
            <% } %>
          </tr>
        <% } %>
      </tbody>
    </table>
  <% }%>
</div>